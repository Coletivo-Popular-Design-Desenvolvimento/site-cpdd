export interface Panel {
    id: number
    label: string
    panelColor: string
    dotColor: string
    textColor: string
    title: string
    description: string
    activitiesTitle: string
    activities: string[]
    image: string
}
