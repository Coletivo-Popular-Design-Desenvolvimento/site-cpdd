export interface Panel {
    id: number
    label: string
    color: string
    dotColor: string
    title: string
    description: string
    activitiesTitle: string
    activities: string[]
    image: string
}
